[package]
name = "mm01-dsp"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "MM-01 synth DSP engine, compiled to WebAssembly"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
